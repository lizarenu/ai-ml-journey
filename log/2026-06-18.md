# 2026-06-18

**Topic:** Backprop from scratch (Phase 0, Thursday)

**Did:**
- Implemented `forward()` in `drills/backprop_from_scratch.py` for a 2-layer XOR network (Z1, A1, Z2, A2). First attempt used `*` instead of `@` for matmul — fixed once I realized the difference.
- Started deriving `backward()` by hand, extending the earlier single-neuron chain-rule derivation to the output layer:
  - `dL/da = 2(a-y)` for sum-of-squares; worked out that with **mean** squared error over `n` samples, only the term involving `a_i` survives differentiation, giving `dL/da_i = 2(a_i - y_i)/n`.
  - `dZ2 = (2/n)(A2-y) * A2*(1-A2)`.

**Confused on:**
- Sigmoid derivative: caught myself writing `da/dz = z(1-z)` (wrong) instead of `a(1-a)` (right) — derivative is in terms of the sigmoid *output*, not its input.
- Matrix shape mechanics for `dW2`: knew `dZ2/dw2_j = a1_j` per sample/weight conceptually, but got the transpose direction wrong (`A1 @ dZ2` instead of `A1.T @ dZ2`) when trying to combine across all samples into the right output shape.

**Stopped at:** mid-derivation of `dW2 = A1.T @ dZ2`, about to confirm the shapes line up `(n_hidden, 1)` before moving on to `db2`, then propagating back into the hidden layer for `dW1`/`db1`.
