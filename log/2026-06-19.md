# 2026-06-19

**Topic:** Backprop wrap-up — concept note + whiteboard Q&A (Phase 0, Friday)

**Did:**
- Finished `backward()` and `train()` in `drills/backprop_from_scratch.py` — completed the gradient derivation from where yesterday's log left off (`dW1`, `db1`, `db2`, full gradient descent loop). XOR network trained and verified converging.
- Lower-energy rest of the day — did a whiteboard Q&A session talking through backprop concepts out loud instead of new drill work, then wrote `concepts/backpropagation.md` (first concept note in the repo).
- Worked through why neural nets need nonlinearity: stacked linear layers with no activation collapse into one linear map; activation functions (applied to `Z`, not the weights) are what stop that collapse. Used XOR (already trained in the drill) as the concrete proof — not linearly separable, so a 0-hidden-layer net can't solve it regardless of training.
- Worked through depth vs. width: multiple smaller layers can be more efficient than one wide layer because each layer builds *new* combinations of the previous layer's activation outputs, rather than just reweighting the same features harder.
- Confirmed the general rule: gradient of a scalar loss w.r.t. any variable has the same shape as that variable — this is literally what would have caught yesterday's `dW2` transpose bug immediately.
- Added the "why subtract the gradient in gradient descent" explanation to the note (moving opposite the direction of steepest increase).

**Confused on / corrected during the session:**
- Conflated "output layer" with "loss function" — output layer produces the prediction; loss function is a separate function comparing that prediction to the true label.
- Said nonlinearity gets "put on the weights" by the activation function — wrong, the activation transforms `Z` (the linear layer's output), not the weights themselves. Weights stay linear coefficients throughout.
- Said derivatives for gradient descent are taken w.r.t. "the input" / loss plotted against "features" — corrected to: derivatives/loss landscape are w.r.t. **parameters** (weights, biases), since those are what gradient descent actually adjusts. Inputs/features are fixed.

**Stopped at:** Concept note for backprop is in a good first-draft state (intuition, math/rules, gotchas, generalization section all filled in, in my own words). Tomorrow starts clean on the next Phase 0 topic (gradient descent variants — momentum, RMSprop, Adam — and L1/L2 regularization).
